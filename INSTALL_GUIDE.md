# 朝日製氷 POS — 店舗運用ガイド

このアプリは、お店のiPadで使う「レジ計算・売上記録」専用のツールです。
インターネットが無い場所でも問題なく動きます。

---

## 1. 必要なもの

- **iPad**（横向きで使えるサイズ。iPad Pro / iPad Air / 標準iPad どれでもOK）
- **自宅のWi-Fi**（最初の1回だけ。アプリのインストールと、年に1〜2回のアップデート時のみ使用）
- **充電器**（営業中は必ず充電しながら使うことを推奨します）

---

## 2. インストール手順（自宅Wi-Fiで1回だけ）

### Step 1. iPadのSafariで以下のURLを開く
```
（デプロイ後にURLをここに記載）
例: https://asahi-seihyou.example.com/
```

### Step 2. 画面下中央の **共有ボタン** をタップ
（四角に上向き矢印が描かれたアイコン）

### Step 3. メニューを下にスクロールして **「ホーム画面に追加」** をタップ

### Step 4. 名前が「朝日製氷」になっていることを確認 → 右上の **「追加」** をタップ

### Step 5. ホーム画面に「朝日製氷」アイコンが現れる
このアイコンをタップして起動します。**Safariからは二度と開かない**でください（Safariの履歴削除でアプリのデータが消える可能性があります）。

---

## 3. 毎日の使い方

### 起動
ホーム画面の朝日製氷アイコンをタップ → 横向きでレジ画面が開きます。

### お会計の流れ
1. 右側の **品書（味の一覧）** からお客様の注文をタップ
2. 同じ味を複数注文されたら、その分だけタップ
3. カートに入った商品で **「ミルク」「スプーン」「特製カップ」** が必要なら、カート内の各カードでタップ
4. 「お預かり」欄にお預かりした金額を入力（**+500 / +1,000 / +5,000 / +10,000** の早押しボタンで加算可能、**消去** で全クリア）
5. 「御釣銭」が自動計算される
6. 中央下の **「御 会 計」** ボタンをタップ → 完了トーストが出てカートが空になる

### お客様が変更を希望されたら
- **「いちご1つだけミルクかけて」** → カート内のいちご行で「ミルク」をタップ。即座に+50円されます。**削除して入れ直す手間は不要です。**

---

## 4. 売上の確認

### 当日の売上を見る
画面右上の **「売上帳」** をタップ。
- かき氷 × N個 = ¥XXX
- ミルク × N個 = ¥XXX
- スプーン × N個 = ¥XXX
- 特製カップ × N個 = ¥XXX
- 合計 ¥XXX

### 過去の売上・期間集計を見る
当日売上モーダルから **「詳細を見る →」** をタップ → 売上詳細画面へ遷移。
- **直近7日 / 30日 / 90日 / 今月 / 全期間** のプリセット
- カレンダーで任意期間を指定 → 「集計」ボタン
- 期間合計、項目別内訳、日別売上が表示される

---

## 5. 設定（品書きの追加・価格の変更）

画面右上の **「設定」** をタップ。

### 品書きの管理
- **追加**：「＋ 新しい品を追加」ボタン → 名前と価格を入力
- **削除**：行ごとに「削除」ボタン（確認あり）
- **並べ替え**：▲▼ボタンで上下移動
- **編集**：名前・価格・色をその場で変更（フォーカスを外した瞬間に自動保存）

### トッピングの管理
- ミルク、スプーン、特製カップの金額をいつでも変更可能
- 新しいトッピングを追加可能
- 削除も可能（過去の売上履歴は壊れません）

> **重要**：価格やトッピングの変更は **次の会計から反映** されます。すでにカートに入っている商品の価格は変わりません（途中変更による事故防止のため）。

---

## 6. データのバックアップ

iPadの故障や紛失に備えて、**月に1回程度** バックアップを取ることを推奨します。

### バックアップ手順
1. 設定画面を開く
2. 「データ管理」セクションの **「JSONバックアップを書き出し」** をタップ
3. iPadの「ファイル」アプリにJSONファイルが保存される
4. AirDropまたはメールで他のデバイスへ送信して保管

### 復元手順
1. 設定画面 → 「JSONバックアップから復元」
2. 保存しておいたJSONファイルを選択
3. 確認ダイアログで「OK」

---

## 7. AI画像の追加（オプション・後日対応可）

各品書きカードに、味のイメージ画像を背景として表示できます。
詳細は同梱の `README_IMAGES.md` をご覧ください。

- 必要なもの: OpenAI API キー（有料、約185円）と自宅Wi-Fi
- 所要時間: 約10分（画像生成・組み込み）

未対応のままでも全機能は使えます。後から追加できます。

---

## 8. トラブル時の対処

### 画面が固まった
ホームボタンを押して別アプリに行き、再度朝日製氷アイコンをタップ → 復帰します。
30分以内なら、カートに入っていた商品も自動復元されます。

### お会計ボタンが押せない
- 品書きから何も選んでいない → 1品以上タップ
- お預かり金額が合計より少ない → 金額を追加

### 「データが満杯に近づいています」と出た
バックアップを書き出してから、**設定 → 「全販売履歴を消去」** で古いデータを削除してください。

### アプリを誤って削除してしまった
1. 自宅Wi-Fiでもう一度URLを開いて再インストール
2. 設定 → 「JSONバックアップから復元」 でデータを戻す
（バックアップを取っていない場合、データは復元できません）

### 営業中にバッテリーが切れた
充電器に繋げば再起動して使えます。会計途中の取引は、完了しなければ売上に記録されません。

---

## 9. アップデート方法

開発側がアプリを更新したとき:

1. 自宅でiPadをWi-Fiに接続
2. 朝日製氷アイコンをタップ → 起動
3. 画面上部に **「更新があります [適用]」** トーストが出る
4. **必ず営業時間外で**「適用」をタップ → 自動で再読込みされて最新版になる

> **注意**: 営業中はトーストを無視してください（「適用」を押すと作業中のカートが消える可能性があります）。

---

## 10. iPadのおすすめ設定

設定アプリで以下を推奨:

- **画面表示と明るさ → 自動ロック → なし** または **15分**
- **アクセシビリティ → アクセスガイド** をON にして、お客様にiPadを渡すときも他のアプリに移動できないようにロック可能
- 営業中は **充電器に接続したまま** 使う（バッテリー消費が大きい）
- 設定アプリで **Safari → 履歴とWebサイトデータを消去** は**絶対に押さないでください**（PWAのデータも消えます）

---

## 11. お問い合わせ

不具合・要望は以下まで:
- メール: （担当連絡先）
- 緊急時: （担当電話）

---

**朝日製氷** © 令和八年
